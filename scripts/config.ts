// Define the default AI model to use
export const DEFAULT_AI_MODEL = 'gpt-4o-mini';
// The name of the port to use for message-passing within the extension
export const FIELD_VALUE_GETTER_PORT_NAME = 'getFieldValues';
